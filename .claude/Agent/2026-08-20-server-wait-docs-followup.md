---
date: 2026-08-20
title: 서버 대기 로딩·실패 알림(8975b28) 이후 낡은 문서·주석 정정
tags: [docs, client, ui, network]
---

# 서버 대기 로딩·실패 알림 도입 이후의 문서 따라잡기

## 목적 / 배경

- `8975b28`(2026-08-19)이 `ServerWaitManager` + `LoadingPresenter`/`NoticePresenter`/`ResultMessages`를
  넣으면서 **`.cs`와 `.meta`만 커밋했다.** `.md`가 한 건도 들어가지 않았다.
- 뒤이은 `1d0be76`이 클라 문서 5개를 따라잡았지만 손이 닿지 않은 곳이 남아,
  문서가 **"실패 사유는 콘솔에만 남는다"** 는 구현 이전 상태를 계속 말하고 있었다.
- 이번 작업은 **코드를 진실로 두고 어긋난 문장만** 고친 것이다. 동작 변경 없음.

## 변경 내용

문서 5 + 주석 3. 자세한 diff는 커밋 참조.

- `Managers/Managers 규칙.md` — **`ServerWaitManager`가 매니저 표에 통째로 빠져 있었다**(4개 → 5개).
  행 추가 + `Begin/Succeed/Fail` 흐름도 절 신설. 하트비트 절에 치명 알림→앱 종료 추가.
- `서버 동작 이해.md` — 하트비트 15초 무응답의 귀결(로그 → 치명 알림 → 종료).
  **T-001을 "아직 미구현"이라 적고 있던 것도 함께 정정**(2026-08-04 완료).
- `패킷 레퍼런스.md` — 거절 사유가 `[↓수신]` 로그에서 끝나지 않고 이벤트 → `ResultMessages` → 알림으로 이어짐.
- `UI/UI 배치 현황.md` — 대기 중 잠금 범위와 탈출 경로를 5초 타임아웃 기준으로 정정(아래 "지뢰" 참조).
- `GameDesign/design/ui/README.md` §5 — `!System Canvas` 접점 행 신설, `PlayerDataLogger` 삭제 조건에서 실패 알림 제외.
- 주석 — `PlayerDataLogger.cs`(삭제 조건) · `PingManager.cs`(무응답의 귀결) · `PlayerDataModel.cs`(끝난 일감 참조 제거).
- `C:\Users\ASUS\Desktop\먼저 할일.MD`(저장소 밖 개인 목록) — A-2·A-5 절 제거.

## 주요 결정 / 근거

- **A-2가 요구한 "조용한 토스트"(소리 없음·자동 소멸)와 실제 구현(사용자 확인형 모달)이 다르다.**
  사용자 판단으로 **의도된 설계 변경**으로 확정하고 미결로 남기지 않았다.
  단 기획 `ui/README.md` 2.3 알림 정책 표는 **게임 진행 알림**만 다루고 요청 실패 축이 없다 —
  그 표에 행을 더할지는 기획 결정이라 손대지 않았다.
- `ui/README.md` 갱신일을 올려 `check-doc-graph.ps1`이 전파 경고 3건
  (게임기획코어·quest·산업레벨)을 냈다. **§5 구현 접점만 바뀌고 기획 규칙·수치는 그대로**라
  전파할 내용이 없다고 판단해 세 문서를 건드리지 않았다. 다음에 같은 경고를 봐도 같은 결론이다.

## 후속 작업 / 주의사항

- ⚠️ **지뢰 — "뒤로가기는 안 잠근다"가 이제 반쪽이다.** `ApplyWaitingLock`은 여전히 뒤로가기를
  잠그지 않지만, **로딩 오버레이가 0.15초 뒤 `blocksRaycasts`로 화면 전체를 덮는다.**
  무응답 시 갇히지 않는 실제 근거는 뒤로가기가 아니라 **5초 타임아웃**이다.
  로딩을 비모달로 바꾸거나 타임아웃을 늘리면 이 균형이 깨진다.
- `PlayerDataLogger` 삭제 조건은 이제 **가챠 결과 팝업 · 위젯 수확 표시** 둘만 남았다
  (가챠는 `tasks/T-031`, 위젯은 스트립 방향 기획 미정).
- `GachaPresenter`는 `GachaCompleted`를 구독하되 **대기만 닫는다** — 보상 표시는 아직 없다.
  "구독이 있으니 화면도 있다"고 읽지 말 것.
