---
id: T-067
제목: 서버 — 장비 뽑기 (가챠 풀에 장비 축 추가)
담당: 서버
상태: 대기
우선순위: 보통
마감: 미정
---

# T-067 서버 — 장비 뽑기 (가챠 풀에 장비 축 추가)

## 배경

**장비를 얻는 정식 경로가 없다.** 지금 장비를 넣는 방법은 치트(`ECheatCommand.GiveEquip`)뿐이라
테스트 계정에서만 장비를 볼 수 있다. 창고 장비 탭은 2026-09-19에 열렸으므로
([T-043](T-043-창고장비특성탭.md)) **받을 그릇은 준비돼 있다.**

가챠는 이미 1회·10회로 돌고 있는데(`GachaService`) **풀이 아이템·캐릭터 둘뿐**이다:

| 축 | 시트 | `GachaRewardType` |
| --- | --- | --- |
| 아이템 | `GachaItemTable` | `Item = 1` |
| 캐릭터 | `GachaCharacterTable` | `Character = 2` |
| **장비** | **없다** | **없다** |

캐릭터 축이 이미 "개체를 발급하고 목록을 다시 내려보내는" 모양이라, 장비도 같은 자리에 붙는다
(`GrantEquip` → `S_EquipSyncResponse`). **새 지급 경로를 만들 필요가 없다.**

## 할 일

- [ ] `GameDesign/Excel` — `GachaEquipTable` 시트 (`GachaId` · `EquipTID` · `Count` · `Weight`).
      `GachaItemTable`과 같은 모양이고 `Ref`는 `EquipTable`을 본다
- [ ] `GachaInfoTable`에 장비 풀 행 추가 (이름 · 비용 — `CostSingle`/`CostMulti`)
- [ ] `GameData.GachaRewardType` · 프로토콜 `EGachaRewardType`에 `Equip = 3` (**값이 1:1이어야 한다** — `PacketEnumTest`가 본다)
- [ ] `GachaRewardInfo`에 `EquipTid` 필드 — 연출용. 지금 `RewardType`이 `ItemId`/`CharacterTid` 중 무엇을 읽을지 정한다
- [ ] `GachaPoolCatalog` — 세 번째 시트를 풀에 조립
- [ ] `GachaService.Draw` — 장비 보상은 `Count`만큼 `GrantEquip`. 캐릭터와 같이 **개체 PK가 DB 발급이라
      응답보다 늦게** `S_EquipSyncResponse`로 내려간다
- [ ] 테스트 · `Server/docs` 갱신

## 완료 조건

더미 클라 또는 클라 가챠 화면에서 장비 풀을 1회·10회 뽑으면 골드가 빠지고,
뽑힌 장비가 **창고 장비 탭에 즉시** 나타난다(재접속 불필요).

## 막고 있는 것 / 선행 일감

- 없음. 받는 쪽(클라 장비 탭·`S_EquipSyncResponse` 처리)은 이미 서 있다.
- ⚠️ 장비 풀의 확률·비용은 **기획 결정**이다 — 테스트값으로 먼저 돌려도 된다([T-008](T-008-희귀도확률.md)과 같은 성격).
- 연관: [T-029](T-029-상자개봉.md) 상자 풀에 장비를 넣을지와 **같은 결정**이다. 한쪽만 정하면 경로가 둘로 갈린다.

## 관련 커밋

- 없음 — 요청: [이슈 #27](https://github.com/JeongTaeWoong99/Windows_simulator/issues/27) (2026-09-19)

## 참고

- 클라가 할 일은 **버튼 2줄 + 보상 분기 하나뿐**이다 —
  `GachaPresenter.draws`(인스펙터 배선)에 `gachaId`·`drawCount` 2줄,
  `GachaResultPresenter.ToSlotData`에 `EGachaRewardType.Equip` 분기.
  `ECheatCommand` 번호와 인자 뜻만 알려 주면 붙인다.
- 치트 지급(`GiveEquip = 8`)은 이미 있다 → [`Server/docs/치트.md`](../Server/docs/치트.md)
