---
id: T-068
제목: 클라 — 계정 레벨 표시 · 특성 트리 화면 · 산업 레벨 선택 칸
담당: 클라
상태: 대기
우선순위: 높음
마감: 미정
---

# T-068 클라 — 계정 레벨 표시 · 특성 트리 화면 · 산업 레벨 선택 칸

## 배경

서버에 **계정 레벨과 특성 트리**가 섰다(2026-09-19, [T-021](T-021-산업레벨해금판정.md)).
캐릭터가 얻은 경험치가 그대로 계정 경험치가 되고, 5레벨마다 특성 포인트 1점을 준다.
포인트로 특성 노드를 찍으면 **산업 레벨(Lv2~5)** 이 열리거나 **산업별 작업속도(+10%씩 5단)** 가 붙는다.

클라에는 아직 이것을 보여 주거나 누를 자리가 없다 — 지금은 상위 산업 레벨을 열 방법이 없다.

## 할 일

- [ ] **계정 레벨 표시** — `S_AccountLevelResponse { Level, Exp, TraitPoint }` 수신(로그인 직후 · 경험치 오를 때 · 포인트 쓸 때). 필요 경험치는 `AccountLevelTable.RequiredExp`
- [ ] **특성 트리 화면** — `UserTraitTable`(노드 · 비용 · 효과) + `UnlockTable`(같은 TID 행의 계정 레벨 · 선행)로 그린다. 찍었는지는 열린 해금 목록(`S_UnlockListResponse` · `S_UnlockResponse`)
  - 요청 `C_UserTraitLearnRequest { UserTraitTID }` → `S_UserTraitLearnResponse { Result, UserTraitTID }`
  - 결과 코드: `NotEnoughTraitPoint` · `UnlockLocked`(계정 레벨·선행 미달) · `AlreadyUnlocked` · `InvalidUserTraitTID`
  - 잠긴 노드도 조건과 함께 보인다(해금 규칙 — 숨기지 않는다)
- [ ] **작업슬롯 세팅 화면의 산업 레벨 선택 칸** — 열린 레벨만 고를 수 있게. 열렸는지 = `IndustryLevelTable.UnlockTID`가 열린 해금 목록에 있는가(Lv1은 `0` = 항상)
- [ ] 속도 특성을 찍으면 슬롯 속도가 `S_WorkStationSlotSyncResponse`로 바뀌어 온다 — 카운트다운이 따라가는지 확인
- [ ] 결과 문구 — `ResultMessages`에 `NotEnoughTraitPoint` · `TraitOnlyUnlock` 추가

## 완료 조건

치트(`GiveAccountExp = 9`)로 계정 레벨을 올린 뒤 특성 트리에서 "낚시 Lv2"를 찍으면 포인트가 1 줄고,
작업슬롯에서 낚시 Lv2를 골라 배치할 수 있다. 속도 노드를 찍으면 그 산업 슬롯의 주기가 짧아진다.

## 막고 있는 것 / 선행 일감

- 없음 — 서버·패킷·데이터가 섰다 ([T-021](T-021-산업레벨해금판정.md))
- 창고 특성 탭([T-043](T-043-창고장비특성탭.md))과 같은 화면으로 둘지는 클라가 정한다

## 관련 커밋

- 없음

## 참고

- 기획: [특성](../GameDesign/design/trait/README.md) 2·3장 · [산업 레벨](../GameDesign/design/gathering/산업레벨.md) 3장 · [해금](../GameDesign/design/unlock/README.md) 4.2
- ⚠️ `PacketId`·`EResultCode` 번호가 바뀌었다(적성 포인트 27·28 → 32·33, 캐릭터 결과 코드 600 → 700) — 미러를 다시 받으면 이름은 그대로라 클라 코드 수정은 없다
