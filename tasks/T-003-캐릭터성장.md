---
id: T-003
제목: 캐릭터 레벨·경험치를 성장에 반영
담당: 서버
상태: 보류
우선순위: 보통
마감: 미정
---

# T-003 캐릭터 레벨·경험치를 성장에 반영

> 2026-09-13 — 경험치 획득식·레벨 곡선을 확정하고 **서버 누적·레벨업·저장·푸시까지 구현했다.**
> 남은 사람의 결정은 **"레벨이 무엇을 올리는가"** 하나이고, 남은 작업은 클라 반영과 레벨 효과다.

## 배경

`t_character`에 `level`·`exp` 컬럼을 만들어 두고 `Character` 객체까지 싣고 있었지만
**아무 데서도 읽지 않았다.** 캐릭터 속도는 `CharacterTable`의 고정 적성값으로만 정해져서,
레벨이 올라도 아무 일도 일어나지 않는다 — 이 부분은 여전히 그렇다(레벨 효과 미정).

## 2026-09-13 확정 — 경험치 획득식 · 레벨 곡선

→ [캐릭터 5.2](../GameDesign/design/character/README.md)

| 항목 | 값 |
| --- | --- |
| 획득 시점 | **판정 1회 성립마다** (`WorkStation.Settle`이 돌려주는 `JudgeCount` 단위) |
| 획득량 | `IndustryLevelTable.ExpPerJudge` — Lv1 1 → Lv5 81 (레벨당 ×3, `RequiredScore`와 같은 등비) |
| 레벨 곡선 | `CharacterLevelTable.RequiredExp` — 그 레벨에 도달하는 데 직전 레벨에서 필요한 양. Lv2 10 → 레벨당 ×1.2 (내림) |
| 만렙 | **100** (테이블 마지막 행). 만렙에서는 더 쌓지 않는다 |
| 레벨업 | 필요치를 채우면 +1, 남은 경험치는 이월. 한 정산에서 여러 레벨 가능 |
| 저장 | `t_character.exp` = **현재 레벨에서 쌓은 양** (누적이 아니다) |

## 2026-09-13 구현 — 서버

| 조각 | 위치 |
| --- | --- |
| 레벨 곡선 보관소 (마지막 행 = 만렙) | `Server/WSGameServer/Common/CharacterLevelCatalog.cs` |
| 경험치 가산 · 레벨업 · 이월 · 만렙 처리 | `Character.GainExp` |
| 정산 시 `JudgeCount × ExpPerJudge`를 배치 캐릭터에 가산 | `User.WorkStation.cs` `SettleWorkStation` → `User.Character.cs` `GrantWorkExp` |
| 확정값 저장 (`UPDATE t_character SET level, exp`) | `Repository/CharacterGrowthRepository.cs` |
| 바뀐 개체 1건 푸시 | `S_CharacterSyncResponse`(PacketId 21) — Unity `ServerPacketHandler.CharacterSynced` 이벤트까지 |
| 테스트 17건 | `CharacterGrowthTest` · `CharacterLevelCatalogTest` · `UserCharacterGrowthTest` |

## 할 일

- [x] 경험치 획득 경로 결정 → **판정 1회마다** (2026-09-13)
- [x] 레벨업 곡선 결정 → `CharacterLevelTable` 100행 (2026-09-13)
- [x] 경험치 누적 · 레벨업 구현 + `t_character` 저장 (2026-09-13)
- [x] 서버 → 클라 푸시 `S_CharacterSyncResponse` + Unity 핸들러·이벤트 (2026-09-13)
- [ ] 클라 반영 → **[T-052](T-052-클라캐릭터성장반영.md)로 분리** (담당 클라 · `PlayerDataModel` 병합 + 레벨·경험치 표시)
- [ ] 레벨 → 적성/속도 기여식 결정 — 적성이 오르는가, 적성과 별개 배수인가 **(⏸ 사람의 결정)**
- [ ] `CharacterTable`과의 관계 정리 — 적성이 테이블 고정값인데 레벨로 바뀌면 무엇이 진실인가
- [ ] 기여식 구현 + `ResolveSlotSpeed` 반영

## 완료 조건

레벨이 오르면 슬롯 속도가 실제로 달라지고, 그 값이 재접속 후에도 유지된다.
(경험치·레벨 자체는 이미 재접속 후 유지된다 — `LoginRepository`가 `level`·`exp`를 읽는다.)

## 막고 있는 것 / 선행 일감

- **레벨이 무엇을 올리는가** — 기획 미정 → [캐릭터 5.3](../GameDesign/design/character/README.md) #1.
  적성을 레벨로 올리면 배치 퍼즐이 무너진다는 경고를 함께 본다
- 클라 반영은 막혀 있지 않다 — 패킷·이벤트가 이미 있다

## 관련 커밋

- `b77bacb` — level/exp 컬럼과 `Character` 객체 도입 (읽지는 않음)
- `5b8939e` — 경험치 곡선 데이터(`ExpPerJudge` · `CharacterLevelTable`) + 기획 문서

## 참고

- `Server/WSGameServer/User/Character/Character.cs` — `GainExp`
- `Server/WSGameServer/User/User.WorkStation.cs` — `SettleWorkStation` · `ResolveExpPerJudge`
- `GameDesign/Excel/Character.xlsx` — `CharacterLevelTable` · `GameDesign/Excel/Industry.xlsx` — `ExpPerJudge`
