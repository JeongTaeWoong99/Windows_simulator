---
id: T-027
제목: 가챠 비용 — 골드를 내고 뽑는다
담당: 서버
상태: 대기
우선순위: 높음
마감: 미정
---

# T-027 가챠 비용 — 골드를 내고 뽑는다

## 배경

**지금 가챠는 공짜다.** `GachaService.Draw`가 횟수와 풀 존재만 검사하고 바로 지급한다.
비용이 없으면 캐릭터를 무한히 뽑을 수 있어 **적성·배치·산업 레벨 해금이 전부 무의미해진다.**

기획의 sink 3종은 *거래 수수료 · 슬롯 확장권 · 가챠 티켓*이었으나
**가챠 비용은 골드 직접으로 확정한다(2026-08-17).** 티켓 획득 경로를 만들지 않아도
`판매 → 골드 → 뽑기`가 곧바로 이어지고, 즉시 판매(T-028)가 만든 골드에 **첫 출구**가 생긴다.

> ⚠️ **sink 목록이 바뀐다.** [거래](../GameDesign/design/trade/README.md) 3.2의 ③ *가챠 티켓*을
> **가챠 비용(골드)** 으로 고쳐야 한다. 티켓 자체를 폐지하는 것은 아니다 —
> 지금 만들지 않을 뿐이다.

## 할 일

- [ ] **엑셀** — 풀 메타 테이블 신설 (`Gacha.xlsx`)
  - [ ] `GachaInfoTable` — `GachaId` · `Name` · `CostCurrency`(eEnum `CurrencyType`) ·
        `CostSingle` · `CostMulti` · `Description`
  - [ ] `GachaId`에 `Ref: GachaTable.GachaId` — **풀이 없는 메타 행을 막는다**
  - [ ] 값은 테스트용으로 넣는다 (밸런스는 T-018·T-009 뒤)
  - [ ] `generate-tables.ps1` 실행 · 엑셀 + 생성물 같은 커밋
- [ ] **서버**
  - [ ] `GachaService.Draw`에 비용 검사·차감을 넣는다 — **지급 전에** 차감한다
  - [ ] 잔액 부족은 새 `EResultCode`(예: `NotEnoughCurrency`)로 거절 — 아무것도 지급하지 않는다
  - [ ] 차감 후 `S_CurrencyResponse`로 잔액을 내려보낸다
  - [ ] 메타가 없는 `GachaId`는 거절한다 (공짜로 새는 구멍을 만들지 않는다)
  - [ ] 테스트 — 잔액 부족 시 **보유 아이템·캐릭터가 그대로**인지 / 10연차 비용이 단차×10이 아닌 값으로 걸리는지
- [ ] **기획 문서** — [거래](../GameDesign/design/trade/README.md) 3.2 sink ③ 갱신 · 전파 검사
- [ ] **프로토콜 미러 커밋**

## 완료 조건

골드가 모자라면 뽑기가 거절되고, 뽑으면 **골드가 줄어든 잔액이 클라에 내려온다.**
공짜로 뽑히는 경로가 하나도 남지 않는다.

## 막고 있는 것 / 선행 일감

- `T-026` — 보상 종류 축이 들어간 뒤에 비용을 얹어야 한 번에 검증된다

## 관련 커밋

- 없음

## 참고

- `Server/WSGameServer/User/Currency/CurrencyWallet.cs` (`TrySpend`가 이미 있다)
- `Server/WSGameServer/Gacha/GachaService.cs`
- [거래 3.2](../GameDesign/design/trade/README.md)
