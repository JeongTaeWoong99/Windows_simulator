---
id: T-044
제목: 클라 — 창고 칸 서버 위치 연동 · 드래그 이동 (자원 · 캐릭터)
담당: 클라
상태: 대기
우선순위: 낮음
마감: 미정
---

# T-044 클라 — 창고 칸 서버 위치 연동 · 드래그 이동 (자원 · 캐릭터)

> [T-036](archive/T-036-창고탭전환.md)에서 갈라져 나왔다. 격자가 **인덱스 기반**으로 바뀌면서
> 자리는 열렸지만, **서버에 슬롯 위치라는 개념이 없어** 지금은 만들 수 없다.
>
> **2026-09-16 범위 확장** — 자원만이 아니라 **캐릭터 탭도** 옮긴다. 그리고 [T-015](art/T-015-산업아이템클라적용.md)에서
> 넣은 **[정렬] 버튼의 임시 기억을 서버 위치로 바꾸는 일**도 여기서 한다(같은 필드를 쓴다).

## 배경

**칸을 끌어 다른 자리로 옮기는 수단이 없다.** 창고 칸의 순서는 지금 **공급자가 내놓는 순서**이고,
그것은 서버가 보내 준 목록 순서다 — 사람이 정한 자리가 아니다.

[정렬] 버튼(2026-09-16)은 누른 순간의 순서를 **클라가 세션 동안만** 기억한다(`StorageSlotSource._place`).
재접속하면 사라진다. 자리의 주인은 서버여야 한다 → [T-058](T-058-창고칸위치서버.md).

### 2026-09-25 — **빈 칸 유지는 클라가 먼저 넣었다**

장착·배치로 개체가 빠질 때 **뒤의 것이 통째로 앞으로 당겨지던 문제**를 먼저 고쳤다.
이제 `StorageSlotSource`가 `Key → 칸`(`_place`)을 들고 있고, 떠난 자리는 **빈 칸으로 남으며**,
들어오는 것은 **앞에서부터 첫 빈 칸**을 가져간다. 격자는 `Get(i)`가 null이면 그 프레임을 비운다.

**그래서 아래 할 일 중 "빈 칸은 빈 채로 그린다"는 이미 됐다.** 남은 것은 그 자리가
**재접속해도 남게** 하는 것 — 즉 `_place`를 서버 칸 번호로 바꾸는 일이다.

## 무엇이 막고 있나

⏸ **[T-058](T-058-창고칸위치서버.md)** — `ItemInfo`·`CharacterInfo`에 칸 위치 필드, 정렬 요청, 자리 이동/교환 요청.

## 할 일

- [ ] ⏸ **서버** — [T-058](T-058-창고칸위치서버.md)
- [x] ~~빈 칸은 빈 채로 그린다~~ — 2026-09-25 완료. 자리 기억(`_place`)과 첫 빈 칸 배치가 들어갔다
- [ ] **[정렬]을 서버 요청으로 바꾼다** — `StorageSlotSource`의 `_place`·`ClearOrder`와 `StorageGridPresenter.OnLoginCompleted`를 걷어내고,
      공급자가 **서버 칸 번호**를 그대로 쓴다 (지금은 세션 동안만 기억한다)
- [ ] 드래그 앤 드롭 — `SlotView`에 포인터 이벤트를 단다 (**자원 · 캐릭터 탭 둘 다**)
      (⚠️ **프리팹을 가챠 결과 팝업도 쓴다.** 거기서는 끌리면 안 되므로
      `SetSubVisible`처럼 **부르는 쪽이 켜고 끄는 스위치**로 만든다)
- [ ] 놓을 자리 판정 · 빈 칸에 놓기 · 다른 칸과 자리 교환
- [ ] **서버 승인 후에 옮긴다** (2026-09-16 사용자 확인) — 놓으면 이동 요청만 보내고, 칸은 응답·위치 푸시가 온 뒤에 바뀐다.
      미리 옮겼다가 거절 시 되돌리는 방식(낙관적 갱신)은 쓰지 않는다 — 자리의 주인은 서버 DB다.
      요청 중에는 끌린 칸을 원래 자리에 둔 채 표시만 한다(연출은 🎨)
- [ ] 우클릭 판매 담기(자원 탭)와 끌기가 부딪히지 않는지 확인

## 완료 조건

[정렬]과 드래그로 정한 자리가 **재로그인해도 그대로**이고, 자원·캐릭터 두 탭 모두 칸을 끌어 옮기거나 바꿀 수 있다.

## 막고 있는 것 / 선행 일감

- [T-058](T-058-창고칸위치서버.md) — 서버 칸 위치 · 정렬 · 이동 패킷

## 참고

- `Assets/Scripts_Client/UI/Storage/StorageGridPresenter/StorageGridPresenter.cs`
  — **격자는 이미 `i번째 항목 → i번째 프레임`이라** 위치 필드만 오면 얹을 수 있다
- `Assets/Scripts_Client/UI/Storage/StorageGridPresenter/StorageSlotSource.cs` — 걷어낼 임시 기억
- `Assets/Scripts_Server/Protocol/PacketInfo.cs` — `ItemInfo`·`CharacterInfo`
