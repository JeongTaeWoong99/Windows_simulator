---
id: T-029
제목: 상자 개봉 — 인벤토리의 상자를 까면 아이템이 나온다
담당: 서버
상태: 대기
우선순위: 보통
마감: 미정
---

# T-029 상자 개봉 — 인벤토리의 상자를 까면 아이템이 나온다

## 배경

**"아이템이 나오는 재미"가 지금 게임에 없다.** 채취는 30초마다 아이템이 조용히 늘 뿐이고,
플레이어가 **직접 여는 순간**이 없다. 상자는 그 순간을 만든다 —
쌓아 뒀다가 한꺼번에 까는 것이 방치형의 대표적인 보상 장치다.

**새 시스템을 만들지 않는다.** 상자는 **인벤토리 아이템**이고, 개봉은 **기존 가챠 풀을 도는 것**이다.
`GachaPoolCatalog`·`WeightedPicker`가 그대로 재사용된다 —
상자마다 다른 풀을 가리키기만 하면 등급별 상자를 얼마든지 늘릴 수 있다.

## 할 일

- [ ] **엑셀**
  - [ ] `Item.xlsx`에 상자 아이템 추가 — `ItemType = Special`, TID 대역은 가챠 전용
        (`100001~`) 뒤에 이어 붙인다. **결번을 되쓰지 않는다**
  - [ ] 상자 → 풀 연결 컬럼 (`OpenGachaId`, `Ref?: GachaTable.GachaId`)
        — 상자가 아닌 아이템은 비워 두므로 `?`를 붙인다
  - [ ] 상자용 가챠 풀 신설 (`GachaTable`)
  - [ ] `generate-tables.ps1` 실행 · 엑셀 + 생성물 같은 커밋
- [ ] **패킷**
  - [ ] `C_ItemUseRequest` — `ItemTID` · `Count`(여러 개 한 번에 까기)
  - [ ] `S_ItemUseResponse` — 결과 코드 · 개봉 결과 목록(연출용) · `ItemChangeInfo`
  - [ ] 개봉 결과는 **가챠 응답과 같은 모양**을 쓴다 (`GachaRewardInfo` 재사용) —
        클라 연출 코드를 두 벌 만들지 않기 위해
- [ ] **서버**
  - [ ] 보유 검증 → 상자 차감 → 풀 추첨 → 지급을 한 트랜잭션으로
  - [ ] `OpenGachaId`가 없는 아이템에 개봉 요청이 오면 거절한다
  - [ ] 상자에서 상자가 나오는 순환을 막는다 (풀 데이터로 막을지 코드로 막을지 정한다)
  - [ ] 테스트 — 미보유 개봉 거절 · 10개 한 번에 개봉 시 차감·지급 수량 일치
- [ ] **프로토콜 미러 커밋**

## 결정이 필요한 것

- **상자를 몇 등급으로 두는가** — 태스크바 히어로식이면 등급별 상자가 여러 개다.
  1종으로 시작해도 되고, 풀만 늘리면 확장된다.
- **상자가 캐릭터를 줄 수 있는가** — T-026에서 보상 종류 축이 생기므로 **구조적으로는 가능하다.**
  캐릭터 가챠와 역할이 겹치지 않게 할지 정한다.

## 완료 조건

더미 클라에서 상자를 10개 까면 **상자가 10개 줄고 아이템이 나온다.**
개봉 결과가 희귀도와 함께 응답에 실려 연출에 쓸 수 있다.

## 막고 있는 것 / 선행 일감

- `T-026` — 보상 종류 축(`RewardType`)이 먼저 들어가야 풀 구조가 한 번에 정리된다

## 관련 커밋

- 없음

## 참고

- `Server/WSGameServer/Gacha/GachaPoolCatalog.cs` — 풀 추첨 재사용 지점
- [아이템 2장](../GameDesign/design/item/README.md) — `ItemUsage` 역할 축 제안
