---
id: T-031
제목: 클라 — 가챠 화면 · 뽑기 결과 연출
담당: 클라
상태: 진행중
우선순위: 보통
마감: 미정
---

# T-031 클라 — 가챠 화면 · 뽑기 결과 연출

## 배경

**"좋은 아이템이 나왔다"를 플레이어가 확인할 수 있어야 뽑기가 재미가 된다.**
서버가 결과를 내려줘도 화면이 없으면 인벤토리 숫자만 조용히 늘 뿐이다.

`S_GachaDrawResponse`는 이미 **연출용 델타(`Rewards`)와 인벤 반영용 누적(`ItemChangeInfos`)을
분리해서** 내려준다 — 뽑힌 순서 그대로 하나씩 열어 보이는 연출이 가능하다.
희귀도(`EGlobalRarity`)도 함께 실려 있고, **등급 색상은 `Enum.xlsx`에 정의돼 있다.**

## 할 일

- [x] 가챠 화면 — 단차/10연차 버튼 (`GachaPresenter`)
- [ ] 가챠 화면 — 풀 목록 · 비용 표시 (버튼 둘만 있고 풀·비용은 아직 없다)
- [ ] 보유 골드 표시 · 부족하면 버튼 비활성 (**클라 판단은 표시용일 뿐, 거절은 서버가 한다**)
- [x] 결과 창 — 뽑힌 목록을 5열 × n행으로 한 번에 표시(수량 표시는 끔), 희귀도 **색상** 적용
      (`UI/System/GachaResultPresenter/`, 칸은 `InventorySlotView` 프리팹 재사용)
- [ ] 결과 **연출** — 한 번에 띄우는 지금 방식 대신 뽑힌 순서대로 하나씩 공개
- [ ] 희귀도 **스프라이트** — 지금은 `RarityPalette`가 색만 돌려준다 (에셋 없음, 코드에 TODO)
- [ ] **캐릭터 결과와 아이템 결과를 한 화면에서 처리한다** (T-026의 보상 종류 축)
- [ ] 뽑은 뒤 캐릭터 목록·인벤토리·골드 표시가 재로그인 없이 갱신되는지 확인
- [ ] 실패 응답(`NotEnoughCurrency` 등) 처리 — 연출을 재생하지 않고 사유를 보여준다

## 완료 조건

화면에서 골드로 10연차를 뽑아 **결과가 순서대로 열리고**,
뽑은 캐릭터가 배치 화면 목록에 바로 나타난다.

## 막고 있는 것 / 선행 일감

- `T-026` · `T-027` — 패킷 모양과 결과 코드가 확정돼야 한다

## 관련 커밋

- `53f71d3` — 가챠 결과 팝업 추가 (결과 창·등급 색·칸 프리팹 재사용)

## 참고

- `Assets/Scripts_Client/폴더 구조.md` — 폴더마다 `<폴더명> 규칙.md`가 있다
- 희귀도 색상: `GameDesign/Excel/Enum.xlsx` (`GlobalRarity`)
