# 서버 동작 이해 (클라이언트 관점 메모)

> 최종 업데이트: 2026-08-20 (하트비트 절 — 치명 알림·앱 종료 반영, T-001 완료 반영)

클라이언트(`Assets/Scripts_Client`) 담당으로 붙이면서, **서버가 어떻게 도는지 이해하려고** 적어 둔 메모.
**"서버를 어떻게 켜고, 패킷이 어떤 길로 들어오고, 값을 어떻게 해석하는가"** 를 다룬다.

> 📖 **기능별로 무엇을 주고받는지**(로그인 · 가챠 · 작업슬롯/채취 · 테이블 · 테스트)는
> [`패킷 레퍼런스.md`](<패킷 레퍼런스.md>)에 있다. 이 문서를 먼저 읽고 그쪽으로 간다.

> **클라 내부 구조는 여기서 다루지 않는다.** 폴더 구성·코딩 규약은
> [`폴더 구조.md`](<폴더 구조.md>), 화면 구조는 [`UI 규칙.md`](<UI/UI 규칙.md>)를 본다.

| 무엇을 찾나 | 절 |
|---|---|
| 서버를 켜고 붙는다 | [서버 실행](#서버-실행) · [접속 정보](#접속-정보) |
| 새 패킷을 붙이려면 어디를 고치나 | [3계층 구조](#3계층-구조--어디에-뭘-쓰나) |
| 패킷이 어떤 순서로 내 코드까지 오나 | [패킷 흐름](#패킷-흐름-수신) |
| ⭐ Count가 총량인가 델타인가 | [수량 값의 의미](#-수량-값의-의미--패킷마다-다르다) |
| `CS0246` · `MIKA001` 이 뜬다 | [`ServerPacketHandler` 예외](#-serverpackethandler는-문서-규칙의-예외다) |
| 왕복이 되는지 확인한다 | [로그로 패킷 흐름 보기](#로그로-패킷-흐름-보기) · [하트비트](#하트비트--끊김-감지) |

---

## 서버 실행

```
dotnet run --project Server/WSGameServer
```

- 콘솔에 `[Server] 10050 포트에서 대기 중...` 뜨면 정상.
- 테스트하려면 **서버를 먼저 켜둔 상태**에서 Unity Play.
- 서버는 `.NET 10` 필요(이미 설치됨).
- ⚠️ 서버가 켜져 있으면 `dotnet build`가 `.exe`를 못 덮어써서 실패한다. 끄고 빌드할 것.

## 접속 정보

- 서버 리슨 포트: **10050** (`Server/WSGameServer/Network/NetworkManager.cs`)
- 클라 접속 포트: **10050** (`Assets/Scripts_Server/Network/MikaNetwork.Unity/NetworkManager.cs`)
- 포트 안 맞으면 `SocketException: 연결 거부` 발생.

---

## 봐야 할 서버 폴더 스크립트

> `Assets/Scripts_Server/...`는 원래 서버 영역. 아래는 협의 후 내가 다루는 것들.

| 파일 | 용도 |
|------|------|
| `Assets/Scripts_Server/Network/MikaNetwork.Unity/ServerPacketHandler.cs` | **응답 패킷 수신 진입점.** `Handle_S_XXX` 핸들러 추가 → 이벤트 발행. (클라 작업 OK — 아래 예외 참조) |
| `Assets/Scripts_Server/Protocol/MikaPacket.cs` | **주고받는 패킷 내용 확인용.** 어떤 필드가 오는지 확인. (직접 수정 금지 — 서버에서 미러링됨) |
| `Assets/Scripts_Server/Protocol/PacketInfo.cs` | 패킷 안 데이터 타입(`ItemInfo`·`GachaRewardInfo` 등) 확인. (직접 수정 금지) |
| `Assets/Scripts_Server/GameData/` | 엑셀에서 생성된 테이블 정의. 아이템·캐릭터 **이름과 적성**이 여기 있다. (직접 수정 금지) |
| `MikaGenerated.GeneratedHandlers` (자동 생성) | 핸들러가 실제 등록됐는지 확인용. 소스 제너레이터가 `[PacketHandler]` 보고 자동 생성. |

### ⭐ `ServerPacketHandler`는 문서 규칙의 예외다

**서버 폴더에 있지만 내가 작업하는 파일이다**(협의됨). 그런데 그 폴더는 네트워크 프레임워크와
자동 미러(`Protocol/`·`GameData/`)가 섞여 있어 **`<폴더명> 규칙.md`를 둘 자리가 없다.**

그래서 이 파일만은 **구조·규칙 설명을 파일 안 주석에 둔다.** 클라 규칙(구조는 md로)의 예외다.

| 주석에 두는 것 | 이유 |
|---|---|
| 구역 구분 규칙 — `#region`마다 **이벤트 선언 → 그 이벤트를 쏘는 핸들러** 순 | 새 패킷을 어디에 넣을지 그 자리에서 봐야 한다 |
| 로그인 1회 수신 세트 | 핸들러를 붙이는 자리라 여기서 필요하다. **[`패킷 레퍼런스.md`](<패킷 레퍼런스.md>)와 중복이지만 의도된 것** |
| "얇게 유지한다"는 원칙 | 여기에 로직을 쓰려는 순간 눈에 들어와야 한다 |

⚠️ **표기 규칙은 클라와 똑같이 지킨다** — XML 서식 태그(`<c>`·`<b>`·`<para>`)를 쓰지 않고
이름은 작은따옴표로 감싼다([`폴더 구조.md`](<폴더 구조.md>) 3-4장).
예외인 것은 **"무엇을 적는가"이지 "어떻게 적는가"가 아니다.**

> 🔧 **`CS0246`이 나면 미러부터 의심한다.** `Protocol/`은 `MikaProtocol` 빌드의 post-build로
> `sync-protocol-to-unity.ps1`이 복사하는 **서버 생성물**이다. 서버가 패킷을 추가하고
> **빌드를 안 돌린 커밋을 받으면 Unity만 깨진다** — 서버는 멀쩡하니 원인이 내 코드로 보인다.
> 저장소 루트에서 아래를 돌려 확인한다:
>
> ```powershell
> powershell -File Server/sync-protocol-to-unity.ps1
> ```
>
> `copy : Protocol\MikaPacket.cs`가 찍히면 **미러가 낡았던 것 = 서버 커밋 누락**이고,
> 전부 `unchanged`면 최신이라 원인이 다른 데 있다.
>
> ⚠️ **이건 내 로컬을 뚫는 용도지, 내가 커밋할 파일이 아니다.** 미러는 서버 담당 폴더라
> 내가 대신 넣기 시작하면 다음에 어긋났을 때 어느 쪽이 진실인지 알 수 없게 된다.
> **누락을 발견하면 서버에 커밋을 요청한다** (실제 사례: 이슈 #12 · `4365d83` → 일감 `T-025`).

> ⚠️ 수신 패킷(`S_*`)에 `[PacketHandler]`가 없으면 **MIKA001 경고**가 뜬다. **현재 남은 경고는 없다.**
>
> **이 경고를 "나중에"로 미루지 말 것.** `S_CharacterListResponse` 핸들러가 없다는 MIKA001을
> 방치한 결과가 곧 "작업슬롯 배치가 전부 거절되는" 버그였다 — 개체 번호를 받을 통로가 막혀 있었다.
> **경고가 곧 기능 누락이다.**

## 3계층 구조 — 어디에 뭘 쓰나

새 패킷을 붙일 때 손대는 곳은 항상 이 3개다.

| 계층 | 파일 | 하는 일 |
|------|------|---------|
| 수신 진입점 | `Scripts_Server/.../ServerPacketHandler.cs` | `[PacketHandler]` + `ClientLog.Info(ClientLog.Recv, …)` 한 줄 + static 이벤트 발행. **얇게.** |
| Model | `Scripts_Client/Managers/PlayerDataModel.cs` | 상태 캐시 + 가공 이벤트. 서버 의존을 여기서 끊는다. **수신 전담 — 송신은 하지 않는다** |
| Presenter | `Scripts_Client/UI/**/*Presenter.cs` | Model 이벤트만 구독해 그린다. **ServerPacketHandler를 직접 보지 않는다.** 요청은 그 요청을 일으킨 Presenter가 직접 보낸다 |

핸들러 등록 코드는 **직접 쓰지 않는다** — `[PacketHandler]` 어트리뷰트만 붙이면 소스 제너레이터가 자동 등록한다.

## 패킷 흐름 (수신)

```
서버  → NetworkManager(PacketReceived)
     → ServerPacketManager(자동 등록 핸들러로 분배)
     → NetworkMessageQueue(메인 스레드로)
     → Update에서 Flush
     → Handle_S_XXX 호출
     → static 이벤트 발행
     → 내 클라 스크립트가 구독해서 처리
```

- 핸들러는 **Unity 메인 스레드**에서 실행됨 → UI 갱신·이벤트 발행 안전.
- `ServerPacketHandler`는 **얇게**(받아서 이벤트만), 실제 처리는 `Scripts_Client`에서.

---

## ⭐ 수량 값의 의미 — 패킷마다 다르다

**가장 자주 틀리는 지점.** 같은 "Count"인데 어떤 건 총량이고 어떤 건 델타다.
잘못 처리하면 수량이 두 배가 되거나 거꾸로 깎인다.

| 패킷 | 필드 | 서버가 넣는 값 | 클라 처리 |
|------|------|---------------|-----------|
| `S_InventoryResponse` | `ItemInfo.Count` | 절대량 (스냅샷) | **교체** |
| `S_UpdateItemResponse` | `ItemChangeInfo.Count` | **갱신 후 누적 총량** | **덮어쓰기** |
| `S_GatherResultResponse` | `ItemChangeInfo.Count` | 위와 동일 (`GainItem` 반환) | **덮어쓰기** |
| `S_GachaDrawResponse` | `ItemChangeInfo.Count` | 위와 동일 (`GainItem` 반환) | **덮어쓰기** |
| `S_GachaDrawResponse` | `GachaRewardInfo.Count` | 이번에 뽑힌 개별 수량 (델타) | **연출에만** — 인벤토리에 반영 금지 |
| `S_CurrencyResponse` | `CurrencyInfo.Amount` | 확정 잔액 | **덮어쓰기** |
| `S_WorkStationSlotsResponse` | `Slots` | 전체 스냅샷 | **교체** |
| `S_WorkStationAssignResponse` | `Slot` | 변경된 한 칸 | **병합** |
| `S_WorkStationSlotSyncResponse` | `Slot` | 변경된 한 칸 (정산·속도 변경 시) | **병합** |

> ⚠️ **`ItemChangeInfo`는 이름과 달리 총량이 들어온다**(`Inventory.AddItem`이 `item.Count`를 넣는다).
> 더하면 두 배가 된다. (`PacketInfo.cs`의 낡은 `// 변경(델타) 전용` 주석은 2026-08-02에 정정됐다)

> ✅ **아이템이 늘어나는 경로는 전부 "덮어쓰기" 하나다.** 예전엔 가챠만 델타 가산이었지만,
> 서버가 `S_GachaDrawResponse`에 `ItemChangeInfos`를 함께 실어 주면서 통일됐다(이슈 #8).
> `GachaRewardInfo`는 원래 정의대로 **연출 전용**(뽑힌 순서·등급 표시)으로 돌아갔다 —
> **인벤토리 갱신에 쓰지 않는다.**

---

## 로그로 패킷 흐름 보기

콘솔에서 `[↑송신]`·`[↓수신]`이 **짝을 이루는지**만 봐도 왕복이 보인다.
`[↑송신]`은 자동이라 송신부에서 따로 로그를 쓰지 않아도 찍힌다.

> `C_PingRequest`/`S_PongResponse`는 **평시 로그에서 제외**된다(5초마다 오가서 콘솔이 덮인다).
> 끊김·복구 순간만 `[연결]`로 남는다 — **조용하면 정상**이다.

태그 전체 규약·레벨·`context` 사용법은 [`Log 규칙.md`](<Log/Log 규칙.md>) 참조.

## 하트비트 — 끊김 감지

- 보냄: `C_PingRequest` (5초마다, `PingManager`)
- 받음: `S_PongResponse`
- **15초** 넘게 Pong이 없으면 `[연결]` 오류를 남기고, `ServerWaitManager.RaiseFatal`로
  **치명 알림을 띄운다 — 확인을 누르면 앱이 종료된다**(에디터에서는 플레이가 멈춘다).
  Pong을 한 번이라도 받았는지로 "도중 끊김"과 "최초 접속 실패"를 갈라 문구가 달라진다.

> ⚠️ **클라가 끊는 것은 앱이지 소켓이 아니다.** 세션은 서버 폴더(`MikaClient`)의 것이라
> `PingManager`는 소켓에 손대지 않는다.
> **서버 쪽 좀비 세션 정리는 T-001에서 끝났다**(2026-08-04 · 이슈 #10 서버 파트) —
> 유휴 세션 스윕(15초)과 같은 Id 중복 로그인 시 기존 세션 kick이 들어가 있다.

---

## 다음은 어디로

| 알고 싶은 것 | 문서 |
|---|---|
| 로그인하면 무엇이 따라오나 · 가챠·채취 패킷 · 카운트다운 계산식 · 테스트 방법 | [`패킷 레퍼런스.md`](<패킷 레퍼런스.md>) |
| 테이블 적재·TID 함정 | [`Data 규칙.md`](<Data/Data 규칙.md>) |
| 어느 화면이 어느 패킷을 다루나 | [`UI 배치 현황.md`](<UI/UI 배치 현황.md>) |
