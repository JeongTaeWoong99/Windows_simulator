---
id: T-006
제목: 작업슬롯 UI
담당: 클라
상태: 대기
우선순위: 높음
마감: 미정
---

# T-006 작업슬롯 UI

## 배경

**서버는 채취가 전부 돌아가는데 클라이언트에 화면이 없다.**
지금 Unity 쪽에는 데스크톱 윈도우 제어(`WindowManager`)와 패킷 테스트 패널만 있고,
게임 화면이라 부를 것이 없다.

서버가 보내는 패킷은 이미 다 준비돼 있다:

| 패킷 | 내용 |
| --- | --- |
| `S_WorkStationSlotsResponse` | 로그인 시 슬롯 전체 스냅샷 |
| `S_WorkStationAssignResponse` | 배치 결과 + 변경된 슬롯 |
| `S_GatherResultResponse` | 채취 결과 푸시 (슬롯별) |
| `C_WorkStationAssignRequest` | 배치 요청 |

> **`WorkStationSlotInfo.CurrentWorkSpeed`가 슬롯의 현재 작업속도**(천분율, 1000 = 1.0배)다.
> 적성 기본값(`WorkSpeedTable.BaseWorkSpeedPermille`)이 아니라 **보정이 전부 적용된 확정값**이므로
> 클라는 이 값만 보면 된다. → [작업슬롯 3.4](../GameDesign/기획/작업슬롯/README.md)

## 할 일

- [ ] 슬롯 목록 표시 — 산업·배치된 캐릭터·진행도
- [ ] 배치 UI — 산업 선택 + 캐릭터 선택 → `C_WorkStationAssignRequest`
- [ ] 카운트다운 — **`CurrentWorkSpeed`와 `JudgeCostUnits`로 로컬 계산**한다.
      서버는 주기(초)를 보내지 않는다 — 슬롯마다 다르고 버프로 바뀌기 때문이다
- [ ] **속도 변경 반영** — `S_WorkStationSlotsResponse`가 다시 오면 `CurrentWorkSpeed`를
      갈아치우고 카운트다운을 재계산한다. 특성·부스트·장비가 붙으면 세션 중에도 값이 변한다
      (서버 `RefreshWorkStationSpeed`가 바뀔 때마다 스냅샷을 다시 밀어 준다)
- [ ] 채취 결과 연출 — `S_GatherResultResponse` 수신 시
- [ ] 적성 0 산업은 배치 시도 자체를 막기 (서버도 막지만 UX상 미리)

## 완료 조건

로그인 → 슬롯에 캐릭터 배치 → 화면에서 카운트다운이 돌고 →
결과가 도착하면 아이템이 늘어나는 것이 눈으로 보인다.

## 막고 있는 것 / 선행 일감

- 없음. 서버 쪽은 준비가 끝났다.
- 확인 편의를 위해 **T-004(전역 배수 복귀)는 이 일감이 끝난 뒤에** 하는 게 좋다

## 관련 커밋

- 없음

## UI 목업

| 파일 | 내용 |
| --- | --- |
| [게임UI목업.html](../GameDesign/기획/게임UI/게임UI목업.html) | **전체 경영 패널** — 캐릭터 / 작업장·인벤토리 / 거래소·상점 3열 |
| [작업슬롯UI목업.html](../GameDesign/기획/게임UI/작업슬롯UI목업.html) | **작업슬롯 전용** — 설정 패널 + 상시 스트립(방향 3안) |

브라우저로 열면 동작한다. 카운트다운은 실제 계산식(`CurrentWorkSpeed`·`JudgeCostUnits`)을 쓴다.

## 참고

- 클라이언트 카운트다운이 서버와 어긋나도 문제되지 않는다 — 연출일 뿐이고
  다음 푸시에서 서버 값으로 맞춰진다 ([작업슬롯 7장](../GameDesign/기획/작업슬롯/README.md))
- `Assets/Scripts_Server/Network/MikaNetwork.Unity/ServerPacketHandler.cs`
